import './index.scss';
import React from 'react';
import CustomIcon from '../customIcon';

const Header = () => {
    return (
        <div className="header">
            <CustomIcon name="talk"/>
        </div>
    );
};

export default Header;
